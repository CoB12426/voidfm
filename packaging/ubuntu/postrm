#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  cat <<'MSG'
VoidFM host was removed. Per-user data is kept; delete it with:
    rm -rf ~/.local/share/voidfm ~/.config/voidfm
MSG
fi
exit 0
